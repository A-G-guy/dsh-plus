/**
 * settings 命名空间字面量：node 半直接注册（0.1.2-alpha.2 起编译期校验）
 * 与浏览器半 settings.plugin.item keyed 槽位配对共用同一字面量（见 notify-email 模式）。
 * @module web-terminal/ns
 */

export const NS_LITERAL = 'dsh-plus-web-terminal'
